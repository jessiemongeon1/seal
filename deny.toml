# This template contains all of the possible sections and their default values

# Note that all fields that take a lint level have these possible values:
# * deny - An error will be produced and the check will fail
# * warn - A warning will be produced, but the check will not fail
# * allow - No warning or error will be produced, though in some cases a note
# will be

# The values provided in this template are the default values that will be used
# when any section or field is not specified in your own configuration

# This section is considered when running `cargo deny check advisories`
# More documentation for the advisories section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html
[advisories]
version = 2
# The path where the advisory database is cloned/fetched into
db-path = "~/.cargo/advisory-db"
# The url(s) of the advisory databases to use
db-urls = ["https://github.com/rustsec/advisory-db"]
# A list of advisory IDs to ignore. Note that ignored advisories will still
# output a note when they are encountered.
ignore = [
    # we don't do RSA signing on Sui (only verifying for zklogin)
    "RUSTSEC-2023-0071",
    # allow yaml-rust being unmaintained
    "RUSTSEC-2024-0320",
    # allow unmaintained proc-macro-error used in transitive dependencies
    "RUSTSEC-2024-0370",

    # allow unmaintained instant crate used in transitive dependencies (backoff, cached, fastrand, parking_lot_*)
    "RUSTSEC-2024-0384",
    # allow unmaintained derivative crate used in transitive dependencies (ark-*)
    "RUSTSEC-2024-0388",

    # `paste` is unmaintained
    "RUSTSEC-2024-0436",
    # `backoff` is unmaintained - no safe upgrade available
    "RUSTSEC-2025-0012",
    # fxhash not maintained
    "RUSTSEC-2025-0057",
    # bincode is unmaintained
    "RUSTSEC-2025-0141",
    # bitmaps, im and sized-chunks (all bodil/im-rs crates) are unmaintained; the GitHub repository
    # was archived on 2026-05-03. Pulled in transitively via im <- fastcrypto-zkp <- Sui crates; no safe upgrade.
    { id = "RUSTSEC-2026-0247", reason = "unmaintained bitmaps pulled in transitively via im <- fastcrypto-zkp <- Sui crates; no safe upgrade" },
    { id = "RUSTSEC-2026-0248", reason = "unmaintained im pulled in transitively via fastcrypto-zkp <- Sui crates; no safe upgrade" },
    { id = "RUSTSEC-2026-0251", reason = "unmaintained sized-chunks pulled in transitively via im <- fastcrypto-zkp <- Sui crates; no safe upgrade" },
    # im 15.1.0 OrdSet insertion aliasing violation (unsound); same unmaintained im as above, no safe upgrade.
    { id = "RUSTSEC-2023-0126", reason = "unsound im OrdSet pulled in transitively via fastcrypto-zkp <- Sui crates; not reachable in our usage, no safe upgrade" },
    # sized-chunks 0.6.5 panic-safety unsoundness in Chunk/RingBuffer/InlineArray; same unmaintained im-rs family, no safe upgrade.
    { id = "RUSTSEC-2026-0255", reason = "unsound sized-chunks pulled in transitively via im <- fastcrypto-zkp <- Sui crates; not reachable in our usage, no safe upgrade" },
    # rand 0.7.3 unsoundness only triggers with a custom logger calling rand::rng() during reseed.
    # Pulled in transitively via fail -> move-vm-runtime -> Sui crates; not reachable in our usage, no safe upgrade.
    { id = "RUSTSEC-2026-0097", reason = "unsound rand 0.7.3 pulled in transitively via Sui's move-vm-runtime; not reachable in our usage, no safe upgrade" },
    # lru 0.13.0 IterMut unsoundness (Stacked Borrows violation).
    # Pulled in transitively via Sui's move-vm-runtime; not reachable in our usage, no safe upgrade.
    { id = "RUSTSEC-2026-0002", reason = "unsound lru 0.13.0 IterMut pulled in transitively via Sui's move-vm-runtime; not reachable in our usage, no safe upgrade" },
    # lru LruCache::pop() panic-safety use-after-free; affects lru 0.10.1/0.13.0/0.16.3, all pinned by
    # Sui crates (sui-core, move-vm-runtime, num-prime <- fastcrypto-vdf); fix requires 0.18.2, no safe upgrade.
    { id = "RUSTSEC-2026-0253", reason = "unsound lru LruCache::pop pulled in transitively via Sui crates; not reachable in our usage, no safe upgrade" },
    # quick-xml 0.38.4 quadratic attribute-duplicate check (CPU DoS on untrusted XML).
    # Pulled in transitively via object_store <- Sui's sui-config; object_store 0.13.1 pins quick-xml ^0.38, so no safe upgrade. We do not parse untrusted XML.
    { id = "RUSTSEC-2026-0194", reason = "quick-xml 0.38.4 quadratic attribute check pulled in transitively via object_store <- Sui's sui-config; object_store pins ^0.38, no safe upgrade; we do not parse untrusted XML" },
    # quick-xml 0.38.4 unbounded NsReader namespace allocation (memory-exhaustion DoS on untrusted XML).
    # Pulled in transitively via object_store <- Sui's sui-config; object_store 0.13.1 pins quick-xml ^0.38, so no safe upgrade. We do not parse untrusted XML.
    { id = "RUSTSEC-2026-0195", reason = "quick-xml 0.38.4 unbounded NsReader namespace allocation pulled in transitively via object_store <- Sui's sui-config; object_store pins ^0.38, no safe upgrade; we do not parse untrusted XML" },
]

# Threshold for security vulnerabilities, any vulnerability with a CVSS score
# lower than the range specified will be ignored. Note that ignored advisories
# will still output a note when they are encountered.
# * None - CVSS Score 0.0
# * Low - CVSS Score 0.1 - 3.9
# * Medium - CVSS Score 4.0 - 6.9
# * High - CVSS Score 7.0 - 8.9
# * Critical - CVSS Score 9.0 - 10.0
#severity-threshold =

# This section is considered when running `cargo deny check licenses`
# More documentation for the licenses section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/licenses/cfg.html
[licenses]
version = 2
# List of explicitly allowed licenses
# See https://spdx.org/licenses/ for list of possible licenses
# [possible values: any SPDX 3.11 short identifier (+ optional exception)].
allow = [
    "MIT",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "Apache-2.0",
    "MPL-2.0",
    "ISC",
    "CC0-1.0",
    "0BSD",
    "Unlicense",
    "BSL-1.0",
    "Unicode-3.0",
    "Apache-2.0 WITH LLVM-exception",
    "Zlib",
    "CDLA-Permissive-2.0"
]
# The confidence threshold for detecting a license from license text.
# The higher the value, the more closely the license text must be to the
# canonical license text of a valid SPDX license file.
# [possible values: any between 0.0 and 1.0].
confidence-threshold = 0.8
# Allow 1 or more licenses on a per-crate basis, so that particular licenses
# aren't accepted for every possible crate as with the normal allow list
exceptions = [
    # Each entry is the crate and version constraint, and its specific allow
    # list
    # { allow = ["Zlib"], name = "adler32", version = "*" },
]

# Some crates don't have (easily) machine readable licensing information,
# adding a clarification entry for it allows you to manually specify the
# licensing information
#[[licenses.clarify]]
# The name of the crate the clarification applies to
#name = "ring"
# The optional version constraint for the crate
#version = "*"
# The SPDX expression for the license requirements of the crate
#expression = "MIT AND ISC AND OpenSSL"
# One or more files in the crate's source used as the "source of truth" for
# the license expression. If the contents match, the clarification will be used
# when running the license check, otherwise the clarification will be ignored
# and the crate will be checked normally, which may produce warnings or errors
# depending on the rest of your configuration
#license-files = [
    # Each entry is a crate relative path, and the (opaque) hash of its contents
    #{ path = "LICENSE", hash = 0xbd0eed23 }
#]
[[licenses.clarify]]
name = "ring"
expression = "LicenseRef-ring"
license-files = [
    { path = "LICENSE", hash = 0xbd0eed23 }
]
[[licenses.clarify]]
name = "encoding_rs"
version = "*"
expression = "(Apache-2.0 OR MIT) AND BSD-3-Clause"
license-files = [
    { path = "COPYRIGHT", hash = 0x39f8ad31 }
]
[[licenses.clarify]]
name = "bls-crypto"
version = "*"
expression = "MIT AND Apache-2.0"
license-files = [
]
[[licenses.clarify]]
name = "target-lexicon"
version = "*"
expression = "Apache-2.0"
license-files = [
]
[[licenses.clarify]]
name = "mvr-types"
expression = "Apache-2.0"
license-files = []

[licenses.private]
# If true, ignores workspace crates that aren't published, or are only
# published to private registries
ignore = false
# One or more private registries that you might publish crates to, if a crate
# is only published to private registries, and ignore is true, the crate will
# not have its license(s) checked
registries = [
    #"https://sekretz.com/registry
]

# This section is considered when running `cargo deny check bans`.
# More documentation about the 'bans' section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/bans/cfg.html
[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "allow"
# Lint level for when a crate version requirement is `*`
wildcards = "allow"
# The graph highlighting used when creating dotgraphs for crates
# with multiple versions
# * lowest-version - The path to the lowest versioned duplicate is highlighted
# * simplest-path - The path to the version with the fewest edges is highlighted
# * all - Both lowest-version and simplest-path are used
highlight = "all"
# List of crates that are allowed. Use with care!
allow = [
    #{ name = "ansi_term", version = "=0.11.0" },
]
# List of crates to deny
deny = [
    # Each entry the name of a crate and a version range. If version is
    # not specified, all versions will be matched.
    #{ name = "ansi_term", version = "=0.11.0" },
    #
    # Wrapper crates can optionally be specified to allow the crate when it
    # is a direct dependency of the otherwise banned crate
    #{ name = "ansi_term", version = "=0.11.0", wrappers = [] },
]
# Certain crates/versions that will be skipped when doing duplicate detection.
skip = [
    #{ name = "ansi_term", version = "=0.11.0" },
]
# Similarly to `skip` allows you to skip certain crates during duplicate
# detection. Unlike skip, it also includes the entire tree of transitive
# dependencies starting at the specified crate, up to a certain depth, which is
# by default infinite
skip-tree = [
    #{ name = "ansi_term", version = "=0.11.0", depth = 20 },
]

# This section is considered when running `cargo deny check sources`.
# More documentation about the 'sources' section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/sources/cfg.html
[sources]
# Lint level for what to happen when a crate from a crate registry that is not
# in the allow list is encountered
unknown-registry = "warn"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "warn"
# List of URLs for allowed crate registries. Defaults to the crates.io index
# if not specified. If it is specified but empty, no registries are allowed.
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# List of URLs for allowed Git repositories
allow-git = [
    "https://github.com/zhiburt/tabled",
]

[sources.allow-org]
github = [
    "mystenmark",
    "mystenlabs",
]
